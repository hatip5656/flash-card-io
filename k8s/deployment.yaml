apiVersion: apps/v1
kind: Deployment
metadata:
  name: flash-card-io
  namespace: flash-card-io
  labels:
    app: flash-card-io
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: flash-card-io
  template:
    metadata:
      labels:
        app: flash-card-io
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: flash-card-io
          image: ghcr.io/hatip5656/flash-card-io:latest
          imagePullPolicy: Always
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          env:
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: flash-card-io-secrets
                  key: TELEGRAM_BOT_TOKEN
            - name: UNSPLASH_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: flash-card-io-secrets
                  key: UNSPLASH_ACCESS_KEY
            - name: EKILEX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: flash-card-io-secrets
                  key: EKILEX_API_KEY
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: flash-card-io-secrets
                  key: DATABASE_URL
            - name: PEXELS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: flash-card-io-secrets
                  key: PEXELS_API_KEY
                  optional: true

            - name: CRON_SCHEDULE
              value: "0 9 * * *"
            - name: CRON_TIMEZONE
              value: "Europe/Tallinn"
            - name: CEFR_LEVELS
              value: "A1,A2,B1,B2"
            - name: FEATURE_TELEGRAM
              value: "true"
            - name: FEATURE_WHATSAPP
              value: "false"
            - name: TTS_API_URL
              value: "http://tts-api:8000"
            - name: CACHE_DIR
              value: "/app/cache"
            - name: TTS_SPEAKER
              value: "mari"
            - name: FEATURE_DEEPFILTER
              value: "true"
            - name: FEATURE_API
              value: "true"
            - name: NODE_OPTIONS
              value: "--max-old-space-size=1024"
          startupProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: cache
              mountPath: /app/cache
          resources:
            requests:
              cpu: 250m
              memory: 1Gi
            limits:
              cpu: 1500m
              memory: 1536Mi
      volumes:
        - name: cache
          persistentVolumeClaim:
            claimName: flash-card-cache
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: flash-card-io-api
  namespace: flash-card-io
spec:
  type: NodePort
  selector:
    app: flash-card-io
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30147
      protocol: TCP
